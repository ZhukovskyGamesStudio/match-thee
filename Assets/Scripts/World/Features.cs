// Переключатели механик на время прототипа: выключенная механика не должна быть видна игроку.
public static class Features {
    // Превращение: клик по предмету в рюкзаке превращает героя в него (корона остаётся сверху).
    // Ряд, в который вошёл герой, исчезает без ресурса, а герой становится собой.
    public const bool Transformation = true;

    // Тратить ли расходуемый ресурс (за ряд из четырёх), когда герой в этой форме исчез в ряду.
    // Смена формы ресурс не тратит: он возвращается в рюкзак. Открытые рядом из пяти виды бесплатны всегда.
    public const bool TransformationCostsResource = true;
}
